import React from 'react';
import { LoadingRingContainer, Ring } from './LoadingRingStyled'; // Adjust the import path accordingly

const LoadingRing = () => {
  return (
    <LoadingRingContainer>
      <Ring />
    </LoadingRingContainer>
  );
};

export default LoadingRing;